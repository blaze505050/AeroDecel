# AeroDecel v5.0 — AI-Driven Aerodynamic Deceleration Analysis
# Install: pip install -r requirements.txt

# ── Core science stack ────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# ── Computer vision (Phase 1) ────────────────────────────────────────────────
opencv-python>=4.8.0
Pillow>=10.0.0

# ── Machine learning (Phase 3 PINN + ensemble) ───────────────────────────────
torch>=2.0.0
scikit-learn>=1.3.0

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.7.0
tqdm>=4.65.0

# ── Optional: AI segmentation (AeroDecel v5.0) ───────────────────────────────
# pip install ultralytics>=8.0.0      # YOLO AI parachute detection
# pip install segment-anything        # SAM zero-shot segmentation

# ── Optional: enhanced features ──────────────────────────────────────────────
# pip install streamlit>=1.32.0       # Web dashboard (app.py)
# pip install emcee>=3.1.4            # Full MCMC chains (bayes_cd.py)
# pip install corner>=2.2.2           # Corner/triangle plots
# pip install pymavlink>=2.4.40       # Pixhawk .bin log parsing
# pip install gpxpy>=1.5.0            # GPX track file parsing
# pip install fitparse>=1.2.0         # Garmin/Suunto .fit files
# pip install rasterio>=1.3.0         # SRTM terrain elevation
# pip install pytest>=7.4.0           # Test suite
# ffmpeg system package for MP4 animation export (optional)
