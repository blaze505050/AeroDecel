# AeroDecel v6.0 — Full Requirements
# Install: pip install -r requirements-full.txt
# Includes ML, dashboard, 3-D viz, REST API, and deployment.

# ── Core physics (always needed) ─────────────────────────────────────────
-r requirements-core.txt

# ── Data & plotting ──────────────────────────────────────────────────────
pandas>=2.0.0
matplotlib>=3.7.0

# ── Web dashboard (Dash) ─────────────────────────────────────────────────
dash>=2.14.0
plotly>=5.17.0

# ── REST API ─────────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0

# ── Machine learning (PINN, neural operators, normalising flows) ─────────
torch>=2.0.0

# ── 3-D visualisation (photorealistic PyVista rendering) ─────────────────
pyvista>=0.42.0
pyvistaqt>=0.11.0

# ── Deployment (Hugging Face Spaces) ────────────────────────────────────
gradio>=4.0.0
huggingface_hub>=0.19.0

# ── Jupyter notebooks ───────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.0.0

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Code quality ─────────────────────────────────────────────────────────
ruff>=0.1.0
